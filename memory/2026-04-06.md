- Added the venue lead capture path: public `/venues` request-demo/contact-sales page, `/api/venues/leads` submission route, and lead qualification helpers in `lib/venue-leads.ts`.
- Added durable lead storage schema via `supabase/migrations/2026040602_venue_leads.sql` with follow-up queue, status, and qualification fields.
- Expanded `/admin/venues` with a recent leads panel so the team can see inbound inquiries and internal routing status.
- Added a homepage CTA to `/venues` so venue operators can find the sales intake flow.
- Validation passed: `npx vitest run app/api/venues/leads/route.test.ts app/venues/page.test.tsx app/admin/venues/page.test.tsx app/admin/page.test.tsx` and `npx tsc --noEmit --pretty false`.
- GitHub update: closed issue #48 (Venue lead capture and qualification).
