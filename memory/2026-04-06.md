- Continued StageSync monetization work on show lifecycle / undo grace / charge protection.
- Added `lib/show-lifecycle.ts` to define the state machine for start, pause, resume, end, and undo transitions, including undo-grace timing and paid-window reuse.
- Wired `/api/shows/[id]/state` through the lifecycle helper and added persistence to the `billing_show_windows` table so undo-grace metadata is retained.
- Exposed an Undo Pause control in the band dashboard when a show is paused.
- Validation passed: `npx vitest run lib/show-lifecycle.test.ts app/api/shows/[id]/state/route.test.ts components/band-dashboard-view.test.tsx lib/entitlements.test.ts` and `npx tsc --noEmit --pretty false`.
- GitHub update: closed issue #47 and moved the corresponding Project 3 card to Done.
- Added StageSync legal surfaces: `/terms`, `/privacy`, and `/support`, linked from the global footer.
- Added a purchase acknowledgment checkbox to the per-event credit flow and enforced it server-side before purchase checkout.
- Validation passed: `npx vitest run app/api/billing/credits/route.test.ts app/band/account/page.test.tsx app/terms/page.test.tsx app/privacy/page.test.tsx app/support/page.test.tsx` and `npx tsc --noEmit --pretty false`.
- GitHub update: closed issues #57 and #58 and moved both Project 3 cards to Done.
- Added analytics reporting foundation: `/admin/analytics` now shows funnel, retention/churn, usage, and storage/export sections.
- Added durable analytics storage schema via `supabase/migrations/2026040601_analytics_reporting.sql` with `analytics_events` and `analytics_daily_rollups` tables.
- Added `lib/analytics-reporting.ts` to define the core reporting sections and expected analytics event types.
- Validation passed: `npx vitest run lib/analytics-reporting.test.ts app/admin/analytics/page.test.tsx` and `npx tsc --noEmit --pretty false`.
- GitHub update: closed issue #52 (Analytics dashboards and reporting).
- Added `lib/analytics-schema.ts` to define the canonical analytics tracking plan: naming conventions, required metadata, prohibited data, and event specs for pricing/show/venue events.
- Surfaced the analytics schema and no-log rules in the admin analytics page alongside the reporting sections.
- Validation passed: `npx vitest run lib/analytics-schema.test.ts lib/analytics-reporting.test.ts app/admin/analytics/page.test.tsx` and `npx tsc --noEmit --pretty false`.
- GitHub update: closed issue #51 (Analytics event schema and tracking plan).
