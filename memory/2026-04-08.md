- Kept the StageSync Tidal UX slice moving instead of stalling.
- Updated `app/api/songs/search/route.ts` so `sourceMode=tidal_playlist` filters the local song search down to imported playlist songs (`source_type = tidal_playlist`), while `tidal_catalog` still uses the live Tidal API.
- Updated `components/tidal-search-panel.tsx` to show a more explicit imported-playlist hint, including the playlist URL when present.
- Added test coverage in `app/api/songs/search/route.test.ts` and `components/tidal-search-panel.test.tsx`.
- Validation passed: `npm test -- --run app/api/songs/search/route.test.ts components/tidal-search-panel.test.tsx`.
