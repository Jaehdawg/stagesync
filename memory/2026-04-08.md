- Kept the StageSync venue-leads/admin slice moving instead of stalling.
- Added `lib/venue-provisioning-drafts.ts` plus `supabase/migrations/2026040801_venue_provisioning_drafts.sql` to store manual venue sales handoff drafts.
- Added `lib/venue-provisioning-trail.ts` plus `supabase/migrations/2026040802_venue_provisioning_events.sql` to track provisioning milestones for each lead/draft.
- Updated `app/api/admin/venue-leads/[id]/route.ts` so every lead update keeps the provisioning draft synchronized, and now also writes a provisioning event trail entry.
- Updated `app/admin/venues/page.tsx` to show the provisioning-draft count and keep the lead-status notice flow on the shared helper.
- Added a new venue provisioning detail page at `app/admin/venue-provisioning/[id]/page.tsx` with a matching test, linked it from the venue ops page, and then made the detail page editable with an update form for status, queue, notes, and milestone.
- Added/updated tests in `lib/venue-provisioning-drafts.test.ts`, `lib/venue-provisioning-trail.test.ts`, `app/api/admin/venue-leads/[id]/route.test.ts`, `app/admin/venues/page.test.tsx`, and `app/admin/venue-provisioning/[id]/page.test.tsx`.
- Validation passed: `npm test -- --run lib/venue-provisioning-drafts.test.ts lib/venue-provisioning-trail.test.ts app/api/admin/venue-leads/[id]/route.test.ts app/admin/venues/page.test.tsx app/admin/venue-provisioning/[id]/page.test.tsx`.
