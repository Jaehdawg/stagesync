- Continued the StageSync song-library polish slice.
- Updated `components/song-library-song-list.tsx` so song rows show human-friendly source badges (`Uploaded`, `Tidal playlist`, `Tidal catalog`, etc.) instead of raw source-type strings.
- Added coverage in `components/song-library-song-list.test.tsx` for tidal catalog and uploaded source labels.
- Validation passed: `npm test -- --run components/song-library-song-list.test.tsx`.
