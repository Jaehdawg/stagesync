- Supabase CLI was installed via Homebrew (`supabase` 2.75.0). `supabase db dump` is blocked here because the CLI wants Docker daemon access; `pg_dump` is also unusable against prod because the server is newer (17.6) than the installed client (16.13).
- Production vs local schema drift remains substantial: production is still on the older `events/show_settings` show model while local migrations/code have moved toward `test_shows/test_show_settings`.
- Production also needed explicit `public` schema grants after the reset; those grants were added separately.
- Live prod errors after deployment confirmed the deployed app was stale: `/api/testing/band-profile` still used `ON CONFLICT`, and `/api/testing/show` still referenced `public.test_shows`.
