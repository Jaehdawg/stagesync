# 2026-04-05

- Professional subscription slice implementation resumed: billing-account sync now derives Professional/free state from billing status plus hosted subscription records, and the band account page uses hosted-checkout wording for action labels and notices.
- Billing intents now round-trip intent-specific placeholder notices (`checkout-pending`, `portal-pending`, `downgrade-pending`, `no-change`) so the UI can distinguish checkout vs portal vs no-op flows later.
- Next 16 build required `searchParams` on `/band/account` to be typed as a Promise; production build now passes after that fix.
