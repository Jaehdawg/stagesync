- Kept the StageSync venue-leads/admin slice moving instead of stalling.
- Added `lib/venue-provisioning-drafts.ts` plus `supabase/migrations/2026040801_venue_provisioning_drafts.sql` to store manual venue sales handoff drafts.
- Updated `app/api/admin/venue-leads/[id]/route.ts` so the `create-draft` action now upserts a provisioning draft alongside the lead update.
- Updated `app/admin/venues/page.tsx` to show the provisioning-draft count and keep the lead-status notice flow on the shared helper.
- Added a new venue provisioning detail page at `app/admin/venue-provisioning/[id]/page.tsx` with a matching test, and linked to it from the venue ops page.
- Added/updated tests in `lib/venue-provisioning-drafts.test.ts`, `app/api/admin/venue-leads/[id]/route.test.ts`, `app/admin/venues/page.test.tsx`, and `app/admin/venue-provisioning/[id]/page.test.tsx`.
- Validation passed: `npm test -- --run lib/venue-provisioning-drafts.test.ts app/api/admin/venue-leads/[id]/route.test.ts app/admin/venues/page.test.tsx app/admin/venue-provisioning/[id]/page.test.tsx`.
