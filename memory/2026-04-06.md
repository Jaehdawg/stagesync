- Continued StageSync monetization work on the PCI / provider boundary.
- Added a shared `lib/payment-boundary.ts` helper that documents hosted payments only, no raw card storage, webhook verification requirements, and provider-metadata-only storage.
- Surfaced the PCI boundary rules on `/terms` and `/admin/billing` so the boundary is visible to users and admins.
- Validation passed: `npx vitest run lib/payment-boundary.test.ts app/terms/page.test.tsx app/admin/billing/page.test.tsx` and `npx tsc --noEmit --pretty false`.
- GitHub update: closed issue #45 and moved the corresponding Project 3 card to Done.