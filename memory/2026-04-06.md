- Added the canonical billing resolver layer in `lib/billing-resolver.ts` to combine account status, subscription plan/status, free-show counts, and purchase eligibility into one snapshot.
- Added a canonical billing audit/event log schema via `supabase/migrations/2026040603_billing_audit_events.sql` for append-only billing and entitlement events.
- Expanded `/admin/billing` with a billing schema / resolver contract section so operators can see the state snapshot, audit event names, and an example audit payload.
- Validation passed: `npx vitest run lib/billing-resolver.test.ts app/admin/billing/page.test.tsx` and `npx tsc --noEmit --pretty false`.
- GitHub update: closed issue #43 (Billing schema and entitlement resolver).
